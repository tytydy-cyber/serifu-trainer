# セリフトレーナー 設計書

小劇場俳優のための台詞覚え支援ツール。台本を取り込み、自分の出番を把握し、マスク＋音声で暗記を確認する。

このファイルは**実装されているもの**を記述する。未実装のものは「8. 未実装」に、意図的にやらないことは「9. やらないこと」にまとめる。

---

## 0. 確定した前提

| 項目 | 決定 | 理由 |
|---|---|---|
| 提供形態 | **PWA（Web アプリ）** | 稽古場ではスマホ、覚え込みは PC と使い分けられる。インストール不要で共演者にも勧めやすい |
| 台本解析 | **ルールベース ＋ 手動修正 UI** | 台本本文が端末外に出ない。未上演作品・稽古台本を扱う以上、外部送信ゼロは要件に近い |
| 読み上げ | **端末内蔵 TTS（Web Speech API）** | 無料・オフライン・即時。抑揚は棒読みだが「きっかけを聞いて自分の台詞を出す」訓練には十分 |
| 暗記判定 | **自己申告のみ** | 音声認識の誤判定ストレスを排除。稽古場の騒音・固有名詞・方言で認識精度が出ないため |

**設計上の含意**：外部サーバを持たない。全データは端末内（IndexedDB）。アカウント・ログイン・共有機能なし。バックアップは JSON エクスポート。AI・機械学習は一切使わない（＝利用に費用が発生しない）。

---

## 1. ユースケース（稽古の実態から）

1. **稽古前日の夜**：明日やる場だけを開き、自分の台詞をマスクして通す → 出番タブ＋マスク練習
2. **移動中の電車**：声は出せない。マスク＋頭出しヒントで目だけで通す → マスク練習
3. **稽古場の隅・本番前**：相手の台詞を TTS で流し、自分の番で止まるので声に出して返す → 音声稽古
4. **台本が改訂された**：新しい稿を取り込み、変わった箇所だけを洗い出して覚え直す → 改訂差分
5. **きっかけ（キュー）が飛ぶ**：直前の相手の語尾だけを出題する → **未実装**（8 章）

---

## 2. 技術構成

```
素の HTML / CSS / JavaScript（ES Modules）— ビルド工程なし
├─ 永続化   IndexedDB（js/db.js の薄いラッパー。外部ライブラリなし）
├─ PDF      pdfjs-dist（vendor/ に同梱）
├─ DOCX     mammoth（vendor/ に同梱）
├─ 音声     Web Speech API（js/tts.js の SpeechEngine で抽象化）
├─ PWA      sw.js（network-first）＋ manifest.json
└─ スタイル  style.css 一枚（ダークモード固定）
```

- **ビルドしない**のは意図的。`index.html` を静的配信するだけで動く（GitHub Pages）。依存を増やさない限りこの性質は保たれる。
- `.nojekyll` が必須。GitHub Pages の既定ビルドは全ファイルを Jekyll に通すため、`vendor/` の圧縮 JS に含まれる `{{` が構文エラーになりデプロイが落ちる。

### ファイル構成

| ファイル | 役割 |
|---|---|
| `js/app.js` | ハッシュルーティング |
| `js/db.js` | IndexedDB ラッパー、エクスポート／インポート |
| `js/extract.js` | ファイル → ページ＋行（**座標から行を組み立てる**のはここ） |
| `js/parser.js` | 行 → ブロック（役名抽出・行分類） |
| `js/appearances.js` | ブロック列 → 出番（区間）の算出 |
| `js/progress.js` | 判定の記録、Leitner 間隔、リセット |
| `js/diff.js` | 改訂差分（LCS） |
| `js/tts.js` | 読み上げ |
| `js/ui.js` | DOM ヘルパー、トースト、確認ダイアログ |
| `js/views/*.js` | 画面ごとの描画 |

---

## 3. データモデル

IndexedDB はスキーマレスに扱っている（`db.js` はストア名と keyPath だけ定義）。フィールドの追加にマイグレーションは要らない。

```ts
interface Script {
  id: string;
  title: string;
  revision: string;            // "第3稿" など。ユーザー入力
  sourceType: 'pdf' | 'docx' | 'txt' | 'paste';
  sourceFileName?: string;
  rawText: string;             // 正規化後の全文（誤パース時の原典）
  pageBreaks: number[];
  createdAt: number;
  performanceDate?: number;    // 本番日。「あと◯日」表示に使う
  parentScriptId?: string;     // 改訂元
  revisionDiff?: RevisionDiff; // 改訂差分の結果（6 章）
  revisionDiffDismissed?: boolean; // 出番タブの改訂サマリーカードを閉じた（6 章）。閉じても revisionDiff 自体は残る
  appearancesEdited?: boolean; // 出番一覧を手で編集した（4-3 参照）
}

interface Role {
  id: string; scriptId: string;
  name: string;                // 表示名（正）
  aliases: string[];           // 「男」「男A」の表記ゆれ
  isMine: boolean;             // 自分の役（複数可 = 二役対応）
  color: string;
}

type BlockType = 'line' | 'direction' | 'heading' | 'cue' | 'unknown';

interface Block {
  id: string; scriptId: string;
  order: number;               // 通し順
  page: number;                // 原本のページ番号
  type: BlockType;
  roleIds?: string[];          // type==='line' のとき。同時発話があるので配列
  text: string;                // 役名プレフィックス除去済み
  inlineDirections: { start: number; end: number }[]; // 「（小声で）」等
  srcStart: number; srcEnd: number;  // rawText 内のオフセット
  confidence: number;          // 0-1。低い順に修正 UI で先頭に出す
}

interface Appearance {         // 「出番」= 自分の台詞が集まった連続区間
  id: string; scriptId: string;
  index: number;               // 0 始まり。ルーティングで使う
  startOrder: number; endOrder: number;
  startPage: number; endPage: number;
  myLineCount: number;
  sceneHeading: string | null; // 直前の見出し（あれば）
  preview: string;             // 自分の最初の台詞の冒頭（見出しが無い台本用）
  label: string;
}

type Recall = 'unseen' | 'missed' | 'shaky' | 'got';

interface Progress {
  blockId: string;             // keyPath
  status: Recall;
  streak: number;              // 連続 'got' 回数
  attempts: number;
  lastReviewedAt: number;
  dueAt: number;               // 次回出題予定（Leitner）
}
```

**設計の要点**：`Block` は必ず `srcStart/srcEnd` で原文に紐づく。パースが間違っても台詞が消えることはなく、いつでも原文と対比できる。

**進捗の消し方**：`Progress` の行を削除する（`unseen` を書き戻さない）。`getProgress` は行が無ければ `unseen` を返すので、「リセットした」と「一度も練習していない」が同じ状態になる。

---

## 4. 取り込みパイプライン

```
ファイル/貼り付け
  → ① テキスト抽出（座標から行を組み立てる）
  → ② 役名候補の抽出とユーザー確定   ← 精度の肝
  → ③ 行分類
  → ④ 手動修正 UI
  → ⑤ 保存 + 出番の自動算出
```

### 4-1. テキスト抽出（`extract.js`）

| 形式 | 手段 |
|---|---|
| PDF（テキスト埋め込み） | `pdfjs-dist` の `getTextContent()` |
| PDF（スキャン画像） | **非対応**。文字が 0 件なら画像 PDF と判定して案内を出す |
| .docx | `mammoth` → テキスト |
| .txt / .md / 貼り付け | そのまま |

**縦書きへの対応**が中心。`getTextContent()` は文字とその座標を返すだけで、行の切れ目も書字方向も教えてくれない。そこで連続する文字の位置関係から「同じ列の続き」「同じ行の続き」「別の行」を判定し、文字の進行方向の多数決で縦書き／横書きを決める。

各行には `lineRole` を付ける。日本語の台本は**行の意味を字下げで表す**（役名は欄外、続きは字下げ）ので、これが分類の材料になる。**`lineRole` が付くのは PDF 由来のページだけ**。貼り付け／.txt／.docx は文字だけで座標情報を持たないため、`lineRole` は常に未設定になり、4-3 の分類は役名一致・見出し・キューなど文言だけのルールにフォールバックする（字下げによるト書き判定・折り返し継続判定は働かない）。

| lineRole | 意味 | 判定 |
|---|---|---|
| `margin` | 新しい発話・ト書きの先頭 | 開始座標が、文書全体で最も多く現れる位置（＝欄外）と一致する |
| `indented` | ト書き | 欄外より内側から始まる |
| `continuation` | 折り返しの続き | **直前の列が最後まで埋まっていた**（＝入り切らずに次の列へ送られた） |

> `lineRole` はあくまで推定であり、**外れることがある**。だから分類（4-3）はこれを鵜呑みにしない。

**既知の落とし穴**（再発させないこと）:
- **ゼロ高さの空白**：pdf.js はルビの脇に、文字を持たない空白アイテムを出す。これは列と列の**あいだ**に置かれるので、無視しないと列が途中で切れて台詞が分断される。
- **ルビ**：本文より小さい文字は本文から除外する（サイズ比で判定）。
- **縦書き用の約物**：`﹁﹂︵︶` は `「」（）` と別のコードポイント。正規化して同一視する。
- **フォント埋め込みの不具合**：日本語の中に ASCII が 1 文字だけ挟まる形が続く PDF は文字化けしている。直せないので警告だけ出す。

### 4-2. 役名候補の抽出（`parser.js`）

行頭から役名らしき文字列を集め、**ユーザーがタップで確定**する。自動判定に頼らず人に決めさせることで、以降の精度が跳ね上がる。

候補にする形（いずれも行頭。装飾記号 `♪▼・` などは先に取り除く）:

| 形 | 例 | 強さ |
|---|---|---|
| `（役名）本文` | `♪（全員）ラーメンを…` | 強 |
| `役名：本文` | `太郎：おはよう` | 強 |
| `役名「本文」` | `太郎「おはよう」` | 強 |
| `役名⇥本文` | タブ区切り | 強 |
| `役名␣本文` | `太郎 おはよう` | 弱 |
| `役名`だけの行 | 改行スタイル | 弱 |

- **強い形**（明示的な区切りがある）は少ない出現回数でも信用する。**弱い形**は繰り返し現れないと採用しない（本文の一文の先頭語に当たってしまうため）。
- **登場人物表があれば最優先**。表に載っていれば 1 回しか喋らない端役でも候補に出す（そうしないと確定画面に現れず、直しようがない）。表に無い候補は「⚠ 登場人物表に無い候補」として既定オフで見せる。
- 表記ゆれ（編集距離 1、包含関係）は同一役の候補としてまとめて提示する。

### 4-3. 行分類（`parser.js` `classifyScript`）

役名が確定した状態で全行を走査する。**判定の順序そのものが仕様**なので、変えるときは理由を確かめること。

```
1. 登場人物表のページ    → ト書き扱い（本文ではない）
2. 折り返しの続き        → ただし先に役名一致を試す（下記★）
3. 見出し（第◯場 など）
4. 技術キュー（M-1, SE, 暗転 など）
5. 役名一致              → セリフ
6. 字下げ（lineRole）    → ト書き
7. 行全体が括弧          → ト書き
8. それ以外              → unknown（要確認）
```

**★ 役名一致はレイアウト推定より強い**。5 が 6 より先、かつ 2 の中でも役名一致を先に試すのはこのため。理由：

- 字下げの判定は「この台本の本文はどこから始まるか」の推定でしかなく、ト書きとセリフを字下げで区別していない台本では大きく外れる（ある台本ではセリフの 9 割がト書きと判定された）。
- 「折り返しの続き」も推定にすぎない。本当の折り返しは文の途中から始まるので、**他人の役名で始まることはない**。役名で始まる行はその推定が外れている。

**unknown の吸収**：どれにも当たらない行は、直前のセリフ／ト書きの続きとみなして連結する。PDF の列送りで切れた断片を拾うため。ただし解決できなかった話者タグらしき行（`父＆麺 ああ！` など）は連結せず unknown のまま残す。

**折り返しと鉤括弧**：長ゼリフは「が最初の列、」が数列先にある。閉じない「も役名一致として認め、連結後に残った片方の括弧は取り除く。

### 4-4. 手動修正 UI

- 種別と役をその場で直せる。種別を変えると確信度は 1（確定）になり、`line` 以外に変えると役の割り当ては外れる。
- 「要確認だけ表示する」— 対象行の**前後 2 行を薄く表示**する。1 行だけ見せられても判断できないため。離れた箇所のあいだは `……` で区切る。
- **↑ 結合** — 直前の行に連結する。PDF の列送りで分断された一文や、判定漏れで千切れた行を手で戻すため。
- **↓ 分離** — テキストエリアのカーソル位置で 1 行を 2 ブロックに割る。自動折りたたみが誤って 2 つの発話を 1 つに繋げたときに使う。分離後の新ブロックは `要確認` から始まる。
- **「◯◯を役にして割り当てる」** — 役名候補の走査では見つかっていたが確定されなかった名前（ウィザードでチェックを外した／表に載っていなかった等）を、要確認行の上でワンタップして役登録とセリフ割り当てを同時に行う。1 件だけでなく、**同じ名前で始まる他の要確認行も同時に一括で割り当てる**（`matchKnownNamePrefix` で再スキャン）。台本のなかで 1 回しか喋らない端役はまずいないので、要確認行の数だけ同じボタンを繰り返し押す手間をなくすため。
- この画面（取り込みウィザードの「仕上げ」ステップ、保存後の「分類を見直す」画面）は共通コンポーネント `js/views/blockFixList.js` を両方から呼び出しており、ロジックの重複がないようにしてある。

### 4-5. 出番の自動算出（`appearances.js`）

```
1. 自分の役の line ブロックを列挙
2. 隣り合う自分の台詞の間隔が閾値（既定 15 ブロック）を超えたら区切る
3. 見出しは必ず区間の境界にする
4. 各区間の前後に他役ブロックを 2 つ含める（出のきっかけごと覚えるため）
```

見出しの無い台本もあるので、カードの見出しは**自分の最初の台詞の冒頭**（`preview`）を既定にする。「どの場面か」より「最初に何を言うか」のほうが実際に記憶を引き出す。

**再計算の条件**：出番の記録が空、または古い形式のとき。ただし `script.appearancesEdited` が立っている（ユーザーが出番を手で削除した）ときは再計算しない。空なのはユーザーの意思なので、勝手に復活させない。自分の役を変更したときはこのフラグを下ろす（区間の前提が変わるため）。

---

## 5. 練習モード

### 5-1. マスク練習

自分の台詞を伏せ、ヒントを段階的に開ける。

| レベル | 表示 | 備考 |
|---|---|---|
| 0 | `███████` | 文字数も伏せる（固定長） |
| 1 | `□□□□、□□□□。` | 文字数がわかる。**句読点はそのまま見せる** |
| 2 | `おれは□□□、□□。` | 頭 3 文字 |
| 3 | 最初の一文 | |
| 4 | 全文 | |

- 句読点を見せるのは、間や文の切れ目という**言い回しを明かさない情報**だから。伏せても難易度が上がるだけで、思い出す訓練にならない。レベル 0 だけは文字数自体を隠すので句読点も伏せる。
- 判定は **言えた / 怪しい / 出なかった** の 3 つ。初回タップで答えを表示し、もう一度タップで記録する（実際に言えたか確かめてから記録するため）。
- 一文以上のヒントを使うと「言えた」は選べない。
- 「通し表示」に切り替えると、出番の全文を自分の台詞だけ伏せた状態で読める。

### 5-2. 音声稽古（TTS）

```
[相手の台詞を読み上げ] → [自分の番：無音の間] → [タップ or 経過で答え表示] → [判定]
```

- 間の長さ：自分の台詞の文字数 × 0.18 秒 ＋ 1 秒。設定で倍率調整（全役共通）。
- 役ごとに声（`voice`）を割り当てて掛け合いを聞き分ける。読み上げ速度（`rate`）は役ごとには設定できず、常に既定値。
- 画面全体タップで次へ（片手・暗所で狙わずに操作できる）。

**PWA / iOS の制約**（回避不能なものは明示する）:

| 制約 | 対応 |
|---|---|
| ユーザー操作起点でないと発話しない | 「稽古を始める」のタップを起点に、以降は `onend` で連鎖 |
| 画面ロック・バックグラウンドで停止 | **成立しないと明示**（README の FAQ に記載） |
| 日本語音声が無い端末がある | `getVoices()` で確認し、無ければ音声モードを無効化して案内 |
| `getVoices()` が初回空 | `voiceschanged` を待つ |

### 5-3. 復習スケジュール（Leitner）

| 判定 | 次回 |
|---|---|
| 出なかった | 同一セッション内で再出題 |
| 怪しい | 翌日 |
| 言えた（連続 1 / 2 / 3+） | 3 日後 / 7 日後 / 14 日後 |

---

## 6. 改訂差分（`diff.js`）

新しい稿を取り込むとき、取り込み画面で「改訂元の台本」を選べる。

1. 各ブロックを `種別＋役名＋本文` の署名にする（役 ID は稿ごとに別物なので**役名**で比較する）
2. 署名列で LCS 差分を取る
3. 素の LCS は「編集された行」を削除＋追加として出すので、同じハンク内の同じ役の削除と追加をペアにして `modified` に直す
4. 進捗の引き継ぎ：`unchanged` はそのまま／`modified` は `shaky` に降格（言い回しが変わった箇所が一番飛ぶ）／`added` は未練習

出番タブの先頭に件数と、変更のあった出番へのリンク、変更点の一覧（旧→新）を出す。カード右上の「✕」で閉じると `script.revisionDiffDismissed` が立ち、以後そのタブを開いても再表示しない。閉じても `revisionDiff` 自体（変更点データ）は消えないので、変更点の内容は失われない。

---

## 7. 画面構成

```
ホーム
├─ 台本一覧（タイトル / 稿 / 改訂版バッジ / 本番まで / 習熟度）
├─ 書き出し（JSON バックアップ）
└─ バックアップ（.json）から復元 ／ ＋ 台本を取り込む

台本詳細（タブは上部に固定表示、**3 タブ**）
├─ 出番     出番カード（進捗バー・マスク練習・音声稽古・⋮ メニュー）
├─ 台本     全文表示。場面ジャンプ（★＝自分が出る場面）、役の凡例、見出し横の「📝 メモ」から場面メモへ
└─ 設定     台本情報 ／ 役の設定 ／ 取り込み結果（分類を見直す）／ データ

場面メモ（台本タブの見出し横「📝 メモ」から遷移。専用タブは無い）
└─ 場面ごとの動き・小道具

分類を見直す（設定タブから遷移。保存済みの台本に対して 4-4 の修正 UI を再度開く）

取り込みウィザード（4 ステップ）
① 台本を読み込む（ファイル／貼り付け・タイトル・稿・本番日・改訂元）
② 役名を確認（表記ゆれの統合）
③ 自分の役
④ 仕上げ（4-4 の修正 UI）
```

**操作の置き場所のルール**：

- **出番ひとつに対する操作**は、その出番カードの `⋮` に置く（進捗をリセット／削除）。
- **台本全体に対する破壊的操作**は、設定タブの「データ」にまとめる（練習記録をすべてリセット／台本を削除）。読むための画面（台本タブ）に破壊的な操作を置かない。
- 元に戻せない操作は必ず確認を挟み、何が消えて何が残るかを文面に書く。

**稽古場を前提とした UI 規約**：

- ダークモード既定（客席・稽古場の隅は暗い）
- 本文は最小 17px、行間 1.8
- タップ領域 44px 以上、主要操作は画面下 1/3（片手・縦持ち）
- 音声モードは画面全体タップで「次へ」
- 長い本文は必ず折り返す（`overflow-wrap`）。伏せ字は同じ文字が続くので、指定を忘れると画面外へ突き抜ける
- 固定表示の位置は**実測して合わせる**（タイトルは長さで 2 行になる）。px 決め打ちは重なりの原因になる

---

## 8. 未実装

| 機能 | 備考 |
|---|---|
| きっかけ（キュー）特訓 | 相手の台詞の末尾だけを出題し、自分の台詞の頭が出るか確認する専用モード |
| 進捗ヒートマップ | ページ単位の習熟度を俯瞰する |
| よく飛ぶ台詞ランキング | `missed` 回数の多い順 |
| 本番逆算ノルマ | 本番日から日割りの目標を出す。本番日の登録と「あと◯日」表示までは実装済み |
| 今日の復習 | `dueAt` は記録しているが、横断して出題する画面が無い |
| Screen Wake Lock | 音声稽古中に画面を保持する |

---

## 9. やらないこと

| 項目 | 理由 |
|---|---|
| スキャン PDF の OCR | 精度とバンドルサイズが割に合わない。テキスト版の入手を案内する |
| 音声認識による自動判定 | 稽古場の騒音・固有名詞・方言で誤判定が多く、ストレスが大きい |
| 台本のクラウド保存・共有 | 未上演作品の著作物を扱う。外部送信ゼロを設計原則とする |
| ログイン・アカウント | 上に同じ。個人ツールとして完結させる |
| 画面オフでの聞き流し | PWA では成立しない |
| 特定の台本だけに効くルール | 「♪ で始まる行は歌」のような書式は台本ごとに違う。汎用的な形（装飾記号を剥がす／括弧つき役名）に落とせないルールは入れない |

---

## 10. 運用上の注意

- **取り込み済みの台本は、解析ルールを直しても自動では直らない**。解析結果を保存する設計のため、直った内容にするには取り込み直す必要がある。
- **データはこの端末のブラウザ内だけにある**。ブラウザの閲覧データ削除で消える。バックアップ（書き出し）を案内すること。
- Service Worker がキャッシュするので、更新は次回起動時に反映される（1 回遅れる）。
- **`guide.html`（アプリ内の使い方ガイド）は `README.md` の「使い方の流れ」以降（開発者向けの「開発」節を除く）を手で転記したもの**。ビルド工程が無いため自動同期はできない。README.md のその範囲を変えたら `guide.html` も合わせて直すこと。`sw.js` の SHELL に含めているので、追加・削除時は VERSION も上げる。
