# セリフトレーナー 設計書

小劇場俳優のための台詞覚え支援ツール。台本を取り込み、自分の出番を把握し、マスク＋音声で暗記を確認する。

## 0. 確定した前提

| 項目 | 決定 | 理由 |
|---|---|---|
| 提供形態 | **PWA（Web アプリ）** | 稽古場ではスマホ、覚え込みは PC と使い分けられる。インストール不要で共演者にも勧めやすい |
| 台本解析 | **ルールベース ＋ 手動修正 UI** | 台本本文が端末外に出ない。未上演作品・稽古台本を扱う以上、外部送信ゼロは要件に近い |
| 読み上げ | **端末内蔵 TTS（Web Speech API）** | 無料・オフライン・即時。抑揚は棒読みだが「きっかけを聞いて自分の台詞を出す」訓練には十分 |
| 暗記判定 | **自己申告のみ** | 音声認識の誤判定ストレスを排除。稽古場の騒音・固有名詞・方言で認識精度が出ないため |

**設計上の含意**：外部サーバを持たない。全データは端末内（IndexedDB）。アカウント・ログイン・共有機能なし。バックアップは JSON エクスポート。

---

## 1. ユースケース（稽古の実態から）

1. **稽古前日の夜**：明日やる場（p.12〜20）だけを開き、自分の台詞をマスクして通す
2. **移動中の電車**：声は出せない。マスク＋頭文字ヒントで目だけで通す
3. **稽古場の隅・本番前**：相手の台詞を TTS で流し、自分の番で止まるので声に出して返す
4. **きっかけ（キュー）が飛ぶ**：直前の相手の語尾だけを出題し、自分の台詞の頭が出るか確認
5. **台本が改訂された**：新しい稿を取り込み、変わった箇所だけを洗い出して覚え直す

> 5 は小劇場では日常的に起きる（稽古期間中に何度も稿が変わる）。**改訂差分は目玉機能として設計に含める**（Phase 3）。

---

## 2. データモデル

```ts
type ScriptId = string; type RoleId = string; type BlockId = string;

interface Script {
  id: ScriptId;
  title: string;
  author?: string;
  revision: string;          // "第3稿" など。ユーザー入力
  parentScriptId?: ScriptId; // 改訂元。差分表示・進捗引き継ぎに使う
  sourceType: 'pdf' | 'docx' | 'txt' | 'paste';
  sourceFileName?: string;
  rawText: string;           // 正規化後の全文（誤パース時の原典）
  pageBreaks: number[];      // rawText 内のページ境界オフセット
  createdAt: number;
  performanceDate?: number;  // 本番日。逆算ノルマに使う
}

interface Role {
  id: RoleId;
  scriptId: ScriptId;
  name: string;              // 表示名（正）
  aliases: string[];         // 「男」「男A」「男１」の表記ゆれ
  isMine: boolean;           // 自分の役（複数可 = 二役対応）
  color: string;
  voice?: { voiceURI: string; rate: number; pitch: number };
}

type BlockType =
  | 'line'      // セリフ
  | 'direction' // ト書き
  | 'heading'   // 第一場 / シーン見出し
  | 'cue'       // M-1、SE、暗転などの技術キュー
  | 'unknown';  // 分類保留（UI で赤く出して修正を促す）

interface Block {
  id: BlockId;
  scriptId: ScriptId;
  order: number;             // 通し順
  page: number;              // 原本のページ番号（稽古で「◯ページ」と言われる）
  type: BlockType;
  roleId?: RoleId;           // type==='line' のとき必須
  text: string;              // 台詞本文（役名プレフィックスは除去済み）
  inlineDirections: { start: number; end: number }[]; // 「（小声で）」等。読み上げ対象外
  srcStart: number;          // rawText 内のオフセット（原典との対応）
  srcEnd: number;
  confidence: number;        // 0-1。低い順に修正 UI で先頭に出す
}

interface Appearance {           // 「出番」= 自分の台詞が集まった連続区間（自動算出）
  id: string;
  scriptId: ScriptId;
  startOrder: number; endOrder: number;
  startPage: number; endPage: number;
  myLineCount: number;
  label: string;               // "出番2  p.12–17  自分の台詞 23"
}

type Recall = 'unseen' | 'missed' | 'shaky' | 'got';

interface Progress {
  blockId: BlockId;
  status: Recall;
  streak: number;              // 連続 'got' 回数
  attempts: number;
  lastReviewedAt: number;
  dueAt: number;               // 次回出題予定（Leitner）
}
```

**設計の要点**：`Block` は必ず `srcStart/srcEnd` で原文に紐づく。パースが間違っても台詞が消えることはなく、いつでも原文と対比できる。これがないと「取り込んだら台詞が1行消えていた」という致命的な事故が起きる。

---

## 3. 取り込みパイプライン

```
ファイル/貼り付け
  → ① テキスト抽出
  → ② 正規化
  → ③ 役名候補の抽出とユーザー確定   ← ここが精度の肝
  → ④ 行分類（役名確定後に再パース）
  → ⑤ 手動修正 UI
  → ⑥ 保存 + 出番の自動算出
```

### ① テキスト抽出

| 形式 | 手段 | 注意 |
|---|---|---|
| PDF（テキスト埋め込み） | `pdfjs-dist` の `getTextContent()` | ページ単位で取れるので `page` を確実に保持できる |
| PDF（スキャン画像） | **非対応**。取り込み時に検出して警告 | OCR は精度・重量ともに割に合わない。「テキスト版をもらってください」と案内 |
| Google ドキュメント | **`.docx` か `.txt` で書き出したものを受ける** | Docs API 連携は OAuth が必要でローカル完結の方針と矛盾する。書き出し手順を画面に明記 |
| .docx | `mammoth.js`（→ HTML → テキスト） | 段落・インデント情報を保持して分類に使う |
| .txt / .md / 直接貼り付け | そのまま | 最も確実。困ったらこれ、という逃げ道を常に用意する |

**縦書き PDF の落とし穴**：`getTextContent()` のアイテム順が視覚順と一致しないことがある。各アイテムの `transform` から座標を取り、縦書き（横書きか縦書きかは文字の進行方向で判定）なら **x 降順 → y 昇順** で並べ替える。ここは取り込みプレビューで必ず目視確認させる。

### ② 正規化

- 全角英数・全角スペース → 半角（役名比較用の正規化キーのみ。表示は原文のまま）
- `\r\n` → `\n`、3行以上の連続空行 → 2行
- ページ境界にセンチネルを入れず、オフセット配列 `pageBreaks` として別持ち（本文を汚さない）

### ③ 役名候補の抽出（最重要）

行頭から役名らしき文字列を集めて頻度順に並べ、**ユーザーが「役名である／ない」をタップで確定**する。ここを自動判定に頼らず人に確定させることで、以降の分類精度が跳ね上がる。

候補抽出のヒューリスティック：

```
候補 = 行頭の、以下いずれかにマッチする 1〜10 文字の文字列
  A) ^(?<name>[^\s：:「」（）]{1,10})\s*[：:]\s*(?<text>.+)$      役名：セリフ
  B) ^(?<name>[^\s「」（）]{1,10})\s*「(?<text>.+)」\s*$          役名「セリフ」
  C) ^(?<name>[^\s「」（）]{1,10})\s*$  かつ 次行が本文           役名だけの行（改行スタイル）
  D) ^(?<name>[^\s]{1,10})\t(?<text>.+)$                          タブ区切り

絞り込み：
  - 出現回数 >= 2
  - 全体行数に対する出現率が極端でない（見出し語の誤検出を除く）
  - 「そして」「しかし」等の接続詞ストップリストを除外
  - 表記ゆれ候補（編集距離 1、または「男」⊂「男A」の包含関係）は
    同一役の alias 候補としてグループ化して提示
```

画面：候補を出現回数つきで一覧 → 役名でないものを外す → 表記ゆれをドラッグでまとめる → **自分の役にチェック**（複数可）。

### ④ 行分類

役名セットが確定した状態で全行を走査する。

```
1. 見出し   ^\s*(第[一二三四五六七八九十0-90-9]+[場幕話]|シーン\s*\d+|○|◯|＊{2,}|─{3,})
2. 技術キュー ^\s*(M\s*[-‐]?\s*\d+|SE|SE\d+|BGM|暗転|明転|溶暗|F\.?O|F\.?I)\b
3. セリフ   確定済み役名 + パターン A/B/C/D にマッチ
            → 役名プレフィックスを除去し text に格納
            → C（役名だけの行）の場合、次の空行または次の役名行までを本文とする
4. ト書き   ^\s*[（(].*[）)]\s*$（全体が括弧）
            または 行頭インデントが本文平均より深い
            または 上記いずれにも当たらず、確定役名を含まない地の文
5. それ以外 → unknown（confidence=0、修正 UI で赤表示）
```

**インライン・ト書き**：セリフ本文中の `（小声で）` `(間)` は `inlineDirections` に範囲を記録。表示は薄いグレー、**読み上げ対象からは除外**。ただし `（間）` だけは読み上げ時に 0.8 秒の無音として扱う（稽古の実感に効く）。

**同時発話**（`A・B「いっせーの！」`）：役名が `・` `／` `＆` で連結された行を検出し、複数 `roleId` を許容する。`Block.roleId` を `roleIds: RoleId[]` にしておくのが安全（上の型は簡略化して単数で書いているが、実装時は配列にする）。

### ⑤ 手動修正 UI

- 全ブロックをリスト表示。左に種別バッジ（色分け）、右に役名チップ
- **`unknown` と `confidence < 0.6` を先頭にまとめる「要確認」タブ**
- タップで種別変更、役名チップで役の付け替え
- 複数行を選択して一括変更
- 「この行以降、同じパターンをすべて適用」
- 常に「原文を見る」で `rawText` の該当箇所と対比できる

---

## 4. 出番（Appearance）の自動算出

自分の台詞の位置から、稽古単位の区間を切り出す。

```
1. isMine な役の line ブロックの order を列挙
2. 隣り合う自分の台詞の間隔（間に挟まる他役ブロック数）が
   閾値 G（デフォルト 15、設定で可変）を超えたら区間を分割
3. 各区間の前後に「助走」として他役ブロックを 2 つ含める
   （出のきっかけを一緒に覚えるため）
4. 見出しブロック（第◯場）は必ず区間の境界にする
```

出力例：`出番2  p.12–17  自分の台詞 23／習熟 61%`

出番一覧はホームの主役級 UI。「今日やるのは出番3だけ」という使い方が最頻。

---

## 5. 練習モード

### 5-1. 台本ビュー（通し読み）

- 全ブロックを縦スクロール。自分の台詞は左に色バー
- ページ番号を固定ヘッダに常時表示（稽古で「◯ページから」に即応）
- ト書きは薄く、技術キューは折りたたみ可
- 「自分の台詞だけ表示」トグル（抜き稿ビュー）

### 5-2. マスク練習

自分の台詞を伏せ、ヒントを段階的に開ける。相手の台詞とト書きは常に見える。

| レベル | 表示 |
|---|---|
| 0 | `████████████`（文字数も伏せる） |
| 1 | `□□□□□□□□`（文字数だけ分かる） |
| 2 | `おれは□□□□□`（頭 2〜3 文字） |
| 3 | 最初の一文だけ表示 |
| 4 | 全文（答え合わせ） |

- タップで 1 段階ずつ開く。「使ったヒントの段階」を記録し、レベル 3 以上を使ったら自動的に `shaky` 扱いにする
- 判定ボタンは画面下部に固定した大きい 3 つ：**言えた / 怪しい / 出なかった**
- 直前の判定を取り消す 1 手戻る（誤タップ対策）

### 5-3. 音声稽古モード（TTS）

```
[相手の台詞を読み上げ]
      ↓ 読み上げ終了
[自分の台詞 → 読み上げず、無音の「間」を置く]
      ↓ 声に出して言う
[タップ or 間の経過] → 答え（自分の台詞）を表示
      ↓
[言えた / 怪しい / 出なかった] → 次へ
```

- **間の長さ**：自分の台詞の文字数 × 0.18 秒（実尺の目安）＋ 1 秒。設定で倍率調整
- 役ごとに voice / rate / pitch を割り当て、複数人の掛け合いを声色で区別
- ト書きは読み上げ ON/OFF を選択（ON のときは声色・速度を明確に変える）
- 自分の台詞も読み上げる「聞き流しモード」を別途用意
- **画面全体をタップ = 次へ**（片手・暗所で狙わずに操作できる）

**PWA / iOS の制約と対策**

| 制約 | 対策 |
|---|---|
| `speechSynthesis` はユーザー操作起点でないと発話しない（iOS Safari） | 「稽古開始」ボタンのタップを起点にし、以降は `onend` で連鎖させて継続する |
| 画面ロック・バックグラウンドで停止する | Screen Wake Lock API で画面を保持。それでも「イヤホンでポケットに入れて聞き流す」は PWA では成立しない。**割り切って明示する** |
| 日本語音声が入っていない端末がある | `getVoices()` で `ja-JP` の有無を確認し、無ければ音声モードを無効化して案内 |
| `getVoices()` が初回空で返る | `voiceschanged` イベントを待つ |
| 長文で発話が途中停止する既知バグ | 台詞を句点で分割し、80〜120 字ごとに `speak()` を分けてキュー投入 |

> TTS は `SpeechEngine` インターフェース（`speak(text, voice) / cancel() / onEnd`）として抽象化しておく。将来クラウド TTS で音声ファイルを事前生成し `<audio>` ＋ MediaSession API で鳴らす方式に差し替えれば、バックグラウンド再生も画面オフの聞き流しも成立する。**今は作らないが、差し替え可能な形にしておく。**

### 5-4. きっかけ（キュー）特訓

小劇場で最も事故るのは「相手の語尾を聞いて自分の頭が出るか」。これを専用モードにする。

- 相手の台詞の **末尾 10〜15 文字だけ**を出題（テキスト or 音声）
- 自分の台詞の **頭だけ**言えれば正解
- 出番の切れ目、場の頭、長い間のあとの台詞を優先的に出題
- 短時間（3 分）で回せるのが利点

### 5-5. 復習スケジュール（Leitner・3 箱）

自己申告の結果から次回出題日を決める。

| 判定 | 次回 |
|---|---|
| 出なかった | 同一セッション内で再出題（3 問後） |
| 怪しい | 翌日 |
| 言えた（streak 1） | 3 日後 |
| 言えた（streak 2） | 7 日後 |
| 言えた（streak 3+） | 14 日後 |

- 本番日を登録すると、**本番前日までに全ブロックが最低 1 回 `got` になる**逆算ノルマを日割り表示
- ホームに「今日の復習 42 問」

---

## 6. 進捗の可視化

- 出番ごとの習熟度バー（got / shaky / missed / unseen の 4 色スタック）
- ページ単位のヒートマップ（台本全体を俯瞰し、赤い場所＝弱点が一目で分かる）
- 「よく飛ぶ台詞」ランキング（`missed` 回数の多い順）— 稽古前に見るリスト
- 直近 14 日の練習量（棒グラフ）

---

## 7. 改訂稿の差分（Phase 3・小劇場の必須機能）

新しい稿を取り込むとき、既存台本を親として指定できる。

1. 新旧のブロック列を **役名＋台詞テキストのハッシュ**で LCS 差分を取る
2. 判定：`unchanged` / `modified`（編集距離で軽微か大幅か判定）/ `added` / `deleted`
3. `unchanged` は **進捗をそのまま引き継ぐ**
4. `modified` は `shaky` に降格（「言い回しが変わった」＝一番飛ぶ箇所）
5. `added` は `unseen`
6. 「変更点だけ練習」モードを差分画面から直接起動

これがあると「第4稿になったけど、どこが変わったんだっけ」が消える。

---

## 8. 画面構成

```
ホーム
├─ 台本一覧（タイトル / 稿 / 本番まであと◯日 / 習熟度）
└─ 今日の復習 [n]問  →  復習セッション

台本詳細
├─ 出番一覧（既定タブ）  ─→ 出番を選ぶ → 練習モード選択
├─ 台本ビュー（通し）
├─ 進捗（ヒートマップ / よく飛ぶ台詞）
└─ 設定（役の声・マスク既定レベル・間の倍率・出番分割の閾値）

取り込みフロー（ウィザード）
① ファイル選択 / 貼り付け
② テキスト抽出プレビュー（縦書き順序の目視確認）
③ 役名の確定 ＋ 表記ゆれ統合 ＋ 自分の役の選択
④ 分類結果の確認（要確認タブ）
⑤ 保存

練習画面（3種：マスク / 音声 / きっかけ）
└─ 下部固定：言えた｜怪しい｜出なかった ／ ヒント ／ 1手戻る
```

**稽古場を前提とした UI 規約**
- ダークモード既定（客席・稽古場の隅は暗い）
- 本文は最小 17px、行間 1.8。可変フォントサイズ設定
- タップ領域 44px 以上、主要操作はすべて画面下 1/3（片手・縦持ち）
- 練習中は誤操作防止のため上部ナビを隠す
- 音声モードは画面全体タップで「次へ」

---

## 9. 技術構成

```
Vite + React + TypeScript
├─ 状態管理     Zustand
├─ 永続化       Dexie（IndexedDB）
├─ PDF          pdfjs-dist
├─ DOCX         mammoth
├─ 音声         Web Speech API（SpeechEngine で抽象化）
├─ PWA          vite-plugin-pwa（Workbox）
└─ スタイル     CSS Modules もしくは Tailwind
```

- **サーバなし**。静的ホスティング（GitHub Pages / Cloudflare Pages）に置くだけ
- Service Worker で app shell をキャッシュ → 完全オフライン動作
- 台本データは IndexedDB のみ。**外部送信ゼロ**を設定画面にも明記する
- バックアップ：全データを JSON で書き出し／読み込み（機種変更・端末故障対策）。**ブラウザのデータ削除で消える**ことをオンボーディングで警告する

---

## 10. 実装フェーズ

**Phase 1 — 取り込みと目で覚える（これだけで実用になる）**
- テキスト貼り付け ＋ PDF 取り込み
- 役名確定 → 行分類 → 手動修正 UI
- 台本ビュー（自分の台詞ハイライト、ページ表示）
- マスク練習（4 段階ヒント）＋ 自己申告の記録

**Phase 2 — 声に出して覚える**
- 出番の自動算出と出番一覧
- TTS 音声稽古モード（役ごとの声色、間の自動調整）
- きっかけ特訓
- Leitner 復習スケジュール、今日の復習

**Phase 3 — 稽古期間を伴走する**
- 改訂稿の差分と進捗引き継ぎ
- .docx 対応、縦書き並べ替えの精度改善
- 進捗ヒートマップ、よく飛ぶ台詞ランキング、本番逆算ノルマ
- JSON エクスポート／インポート、Wake Lock

---

## 11. 明示的に「やらない」こと

| 項目 | 理由 |
|---|---|
| スキャン PDF の OCR | 精度とバンドルサイズが割に合わない。テキスト版の入手を案内する |
| 音声認識による自動判定 | 稽古場の騒音・固有名詞・方言で誤判定が多く、ストレスが大きい |
| 台本のクラウド保存・共有 | 未上演作品の著作物を扱う。外部送信ゼロを設計原則とする |
| ログイン・アカウント | 上に同じ。個人ツールとして完結させる |
| 画面オフでの聞き流し | PWA では成立しない。将来クラウド TTS ＋ 音声ファイル化で対応可能な形にだけしておく |

---

## 12. 実装前に決め切れていない点（着手時に判断）

1. **出番の分割閾値 G のデフォルト値** — 実際の台本で試して調整する。設定で可変にしておけばリスクは小さい
2. **一人二役の扱い** — `isMine` を複数役に付けられる設計にはしたが、練習中に「今どちらの役か」を UI でどう出すかは実装時に詰める
3. **ページ番号の起点** — PDF の物理ページと台本上の印字ページがずれることがある（表紙・人物表）。取り込み時に「本文 1 ページ目はどれか」を指定するオフセット設定を入れる
